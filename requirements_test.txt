pytest-homeassistant-custom-component
# Transitive requirements pulled in when our config flow / websocket_api
# import homeassistant.components.assist_pipeline (which eagerly imports
# conversation, tts, and its own vad/noise deps). pytest-homeassistant-
# custom-component does not install these per-component requirements
# itself, so they have to be pinned here. Versions must match whatever
# homeassistant release pytest-homeassistant-custom-component actually
# pulls in (currently 2026.2.3), not core's `dev` branch HEAD - the two
# drift and a `dev`-branch hassil doesn't ship the modules the pinned
# homeassistant release imports (e.g. hassil.fuzzy).
hassil==3.5.0
home-assistant-intents==2026.1.28
pymicro-vad==1.0.1
pyspeex-noise==1.0.2
mutagen==1.47.0
# tts imports homeassistant.components.ffmpeg at module level, which in
# turn imports the haffmpeg module from this package.
ha-ffmpeg==3.2.2
